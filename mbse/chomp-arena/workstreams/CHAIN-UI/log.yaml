# CHAIN-UI — collaboration log
# Append-only. One entry per agent action. Never rewrite an existing entry;
# correct it with a new entry that says what changed and why.
# requirement_count and requirements are checked against the requirements tree
# by validate.py — they cannot silently drift.

chain: CHAIN-UI
owner: Codex (integration)
scope: The HUD, touch controls on the iPad, colour-coded gate readiness, off-deck
  player markers. Display only — it holds no state.
requirement_count: 8
requirements:
- CHOMP-STK-001
- CHOMP-SYS-030
- CHOMP-SYS-031
- CHOMP-SYS-032
- CHOMP-SYS-052
- CHOMP-SYS-055
- CHOMP-SYS-056
- CHOMP-SYS-057
phases:
- v1
status: IN_PROGRESS
entries:
- date: '2026-08-25'
  agent: Claude (requirements session)
  action: ESTABLISHED
  requirements:
  - CHOMP-STK-001
  - CHOMP-SYS-030
  - CHOMP-SYS-031
  - CHOMP-SYS-032
  - CHOMP-SYS-052
  summary: Chain established with 5 requirements across v1. Nothing built. See 02_requirements/requirements.yaml
    for the shall statements and 04_verification/test_cases.yaml for how each is
    verified.
  status: OPEN
- date: '2026-08-25'
  agent: Claude (tree owner)
  action: NOTE
  requirements: [CHOMP-SYS-032, CHOMP-SYS-031]
  summary: >
    Control scheme decided and specified (D-CHOMP-015): hold left or right of a
    20% dead zone to steer, proportional to distance out to full lock, always
    driving forward, double-tap the far side to flip 180. No accelerator, no
    brake, no action buttons anywhere — banking and gate passage are
    consequences of driving somewhere. See 03_architecture/control_scheme.md
    and ChompConfig.Controls.
  status: OPEN
  handoff: >
    The HUD must keep the bottom-left and bottom-right corners clear — 30% of
    width by 30% of height each — because that is where thumbs live. Bar, carry
    and gate readiness go top-left and top-centre, standings top-right, nothing
    along the bottom edge. Both sides held at once must cancel to straight,
    which is what a panicking player does.
- date: '2026-08-25'
  agent: Claude (engine)
  action: DELIVERED
  requirements: [CHOMP-SYS-032]
  summary: >
    InputController.client.lua implements hold-to-turn with the 20% dead zone
    and the linear ramp to full lock, multi-touch summed and clamped so both
    sides held cancels to straight, double-tap flip on both touch and keyboard,
    and keyboard steering for building in Studio. It sends intent only, at two
    thirds of the remote rate limit, and only when the value has actually
    changed.
  status: OPEN
  handoff: >
    The HUD does not exist yet. When it does, keep the bottom corners clear —
    30% of width by 30% of height each — because that is where thumbs live.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-032]
  summary: >
    Owner decision after the first drivable build: the vehicle now moves only
    while a steering input is held, and coasts to a stop when released
    (D-CHOMP-026, superseding the always-forward half of D-CHOMP-015).
    InputController publishes state.driving; VehicleController ramps speed by
    Acceleration and Braking and scales the heading vector by the result, so the
    throttle rides on MoveDirection magnitude and WalkSpeed stays the server's
    number. control_scheme.md and BUILDING.md amended to match.
  status: OPEN
  handoff: >
    NOT yet played. The touch half is untested and matters more than the
    keyboard half: on iPad, holding to steer must also be what drives, and the
    dead zone means a touch near screen centre steers barely at all — check that
    such a touch still drives at full speed rather than crawling, because
    throttle is gated on driving being true, not on steer magnitude. If that
    feels wrong, the fix is in InputController where driving is computed, not in
    VehicleController.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-032]
  summary: >
    D-CHOMP-026 was wrong and was caught in one playtest: gating movement on the
    steering hold means every path is an arc and driving straight is impossible.
    Replaced with a single floating stick (D-CHOMP-027) — forward drives, left
    and right steer proportionally, back reverses and the vehicle swings to face
    its travel direction. InputController rewritten; VehicleController now ramps
    a signed speed by Acceleration away from zero and Braking toward it.
    ChompConfig gains StickRadiusFraction, StickDeadZoneFraction and
    ReverseSpeedFraction. control_scheme.md and BUILDING.md rewritten.
  status: OPEN
  handoff: >
    NOT yet played, and the touch half is entirely unexercised — Studio only
    tests the keyboard path. Three things need a device: that the floating
    anchor survives a thumb dragging beyond the stick radius, that a second
    finger being ignored is right rather than annoying, and whether reversing
    should mirror the steering axis. Reverse currently does not mirror, so
    holding back and left swings the nose the same way it would going forward;
    that reads as "turn around" but has not been felt by anyone.
    The wire format is unchanged: throttle is client-local and is NOT sent on
    SetInputDirection, because nothing server-side consumes input yet and
    widening the remote means amending service_contracts.md and extending
    CHOMP-TC-042 in the same commit.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-032]
  summary: >
    CHOMP-TC-040 could not be run as written: BUILDING.md sent the tester to the
    Studio command bar, and the iPad it names as the reference platform has no
    command bar. Added CameraReadout.client.lua (D-CHOMP-029) — worst occlusion,
    limit, breach count and a live HIDDEN duration at the top of the screen, red
    on CHOMP-SYS-051 failure, with a RESET button. Reads a new silent
    _G.ChompCameraStats accessor rather than the printing report, so polling it
    does not bury the breach warnings. Gated on ChompConfig.Debug.CameraReadout.
  status: OPEN
  handoff: >
    The readout itself is unplayed on a device — it is a HUD written by someone
    who cannot see a HUD. Check on the iPad that it is legible at arm's length,
    that it does not sit under the notch or a rounded corner despite
    IgnoreGuiInset, and that RESET is reachable without the thumb crossing the
    stick. If the frame overlaps anything, its position is one UDim2 at the top
    of the file. Turn CameraReadout off in ChompConfig before this is ever
    played for fun rather than measured.
- date: '2026-08-25'
  agent: Codex (HUD/gameplay requirements)
  action: NOTE
  requirements: [CHOMP-SYS-055, CHOMP-SYS-056, CHOMP-SYS-057]
  summary: >
    Added child-facing requirements for stable play-HUD zones, distinct feedback
    for carried points, safe dollars, battle charge and Power, one contextual
    next objective, and a garage comparison that shows five relative vehicle
    bars plus the exact dollars remaining before an unlock. These are display
    requirements only; the HUD remains non-authoritative under CHOMP-SYS-030.
  status: OPEN
  handoff: >
    Review the proposed layout against the iPad thumb zones, then prototype the
    HUD with server-replicated values. CHOMP-TC-045 and CHOMP-TC-046 require a
    child-facing demonstration before approval.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-032]
  summary: >
    Fourth and hopefully final control scheme: grid driving (D-CHOMP-033). A
    direction is the drive command — hold it and the vehicle goes that way,
    snapping instantly when stopped and turning at BaseTurn when moving. No
    separate throttle, no reverse, no flip gesture. VehicleController now tracks
    a desired heading rather than a steering scalar; InputController still
    publishes only two axes. control_scheme.md and BUILDING.md rewritten.
  status: OPEN
  handoff: >
    NOT yet played. Two things to watch. First, the most-recent-press rule: two
    keys held at once follows the newer one, and if that reads wrong when
    cutting a corner the logic is entirely inside updateDesired. Second, the
    snap threshold — Movement.SnapBelowSpeed is 1.5 studs/s, and if the vehicle
    snaps while still visibly rolling, lower it; if reorienting feels like it
    needs a full stop first, raise it.
    Touch is still unexercised on a device. The stick now picks a cardinal from
    whichever axis crossed the press threshold most recently, which is a
    different feel from proportional steering and has only ever been reasoned
    about, never felt.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-055, CHOMP-SYS-056]
  summary: >
    Play HUD built to Codex's zones (D-CHOMP-050): carried top-left in pink,
    banked top-right in gold, one urgency-ranked objective top-centre, held item
    and charges beneath the carry, lower corners kept clear. A theft flashes the
    screen red. Every value is a server-written attribute, read-only here.
  status: OPEN
  handoff: >
    CHOMP-TC-045 is the child-facing demonstration and this is the thing to run
    it against. Two of its pass criteria are already testable: whether carried
    and banked are correctly identified as risky and safe, and whether the HUD
    overlaps either lower 30% thumb zone.
    CHOMP-SYS-057, the garage screen with five comparison bars, is NOT built.
    Nor is CHOMP-SYS-056's requirement that values not be distinguished by
    colour alone - carried and banked currently differ by colour, position AND
    label, which satisfies it, but nothing tests that and the criterion is still
    missing from CHOMP-TC-045.
- date: '2026-08-25'
  agent: Claude (local session)
  action: NOTE
  requirements: [CHOMP-SYS-055, CHOMP-SYS-032]
  summary: >
    TWO REQUIREMENT CONFLICTS, opened deliberately rather than ignored.
    The HUD values moved to a bottom-right stack (D-CHOMP-053) because Roblox's
    own chrome - menu, chat, close - sits on top of the top corners and made
    both panels unreadable. CHOMP-SYS-055 specifies top-left and top-right and
    was written before anyone saw the game with that UI present.
    The new position also enters the lower-right zone CHOMP-SYS-032 reserves for
    thumbs. On desktop that is free; on an iPad it will not be.
  status: OPEN
  handoff: >
    Someone has to decide which is wrong: the requirement or the layout. The
    honest options are to amend CHOMP-SYS-055 to specify "clear of platform
    chrome" rather than naming corners, or to keep the top zones and inset them
    below Roblox's topbar. Do not leave the code and the tree disagreeing.
    On touch this stack is almost certainly wrong and CHOMP-TC-045 will say so.
- date: '2026-08-26'
  agent: Claude (local session)
  action: DECIDED
  requirements: [CHOMP-SYS-032, CHOMP-SYS-055, CHOMP-SYS-056]
  summary: >
    Belt slots 42x52 -> 64x68 with the full item label instead of a
    four-character truncation, an item-coloured bar and slot number, and a
    17-point cap so five slots read as one row. Charge button shows a
    percentage, measures progress toward the JUMP rather than the cap, and
    pulses red on a press it cannot honour. Friendly-fire button text was
    P.Brick on P.Floor - dark purple on near-black - and is now legible, states
    OFF explicitly, and reads SOLO - NO PVP with no second player (D-CHOMP-064).
  status: OPEN
  handoff: >
    The lesson in all three: every one of these controls WORKED. They read as
    broken because they could not be seen, could not be reached, or never
    answered a press. When an owner reports a control is disabled, check
    contrast and reachability before checking the handler.
    Belt strip is now 334px wide against the value stack's 236, both anchored
    bottom-right. It fits landscape iPad comfortably; portrait has not been
    checked and CHOMP-TC-040 is still unrun.
- date: '2026-08-27'
  agent: Claude (local session)
  action: NOTE
  requirements: [CHOMP-SYS-055, CHOMP-SYS-056]
  summary: >
    BUILDING.md's play and test sections and CHOMP-TC-040 rewritten to match the
    built game. Both still described two decks, a bridge and a tower core that
    D-CHOMP-046 deleted, and BUILDING.md still told the tester "a second finger
    is ignored" - the opposite of true since second-finger fire landed. Following
    either as written would have tested a game that does not exist.
  status: OPEN
  handoff: >
    TC-040's procedure is now a single-level occlusion circuit read off the
    on-screen readout (D-CHOMP-029), with jump takeoff and landing added because
    the charge jump is the only vertical motion left on the map.
    Recorded honestly rather than papered over: the bay sequence, the deployment
    gate, profile persistence, the degraded read-only path and two-finger fire
    are all BUILT AND UNSPECIFIED - no requirement covers them and no test case
    verifies them. BUILDING.md now says so in its own section rather than
    implying the list is coverage.
    Note for whoever writes those requirements: the launch bay as built is the
    home garage pad, not the four-lane bay with turntable and equipment rack in
    LAUNCH-READINESS. Driving off the yellow pad IS the deployment gate. Specify
    what exists, or build what is specified, but the two are not the same today.
- date: '2026-08-27'
  agent: Claude (local session)
  action: NOTE
  requirements: [CHOMP-SYS-030]
  summary: >
    CORRECTION to the ESTABLISHED entry above, same fault as CHAIN-COMBAT. It
    read "Chain established with 5 requirements" while aliasing the header, so
    appending CHOMP-SYS-055 to -057 retroactively made it claim 8. The entry now
    carries the literal five it was established with, recovered from git.
  status: OPEN
  handoff: >
    The header today holds 8 and that is correct. Only the historical entry was
    wrong. See the CHAIN-COMBAT note of the same date for the general rule and
    the validate.py check that now enforces it.
- date: '2026-08-28'
  agent: Codex
  action: DELIVERED
  requirements: [CHOMP-SYS-032, CHOMP-SYS-055, CHOMP-SYS-056]
  summary: >
    Release 0.3.6-alpha removes the development camera readout and reset control,
    removes the solo/friendly-fire HUD button, and moves Roblox platform chat to
    the right. The utility belt now sits below the money panel, with health above
    that group, and the jump charge button has a persistent high-contrast outline,
    readable live level/progress, and an active touch target in every charge state.
  status: OPEN
  handoff: >
    Rebuilt build/ChompArena.rbxlx from source. Verify the right-aligned platform
    chat at the target Studio viewport and confirm the bottom-right stack remains
    clear of Roblox controls on touch hardware.
- date: '2026-08-28'
  agent: Codex
  action: DELIVERED
  requirements: [CHOMP-SYS-055]
  summary: >
    Release 0.3.7-alpha removes the persistent BANK IT - GARAGE objective. During
    ordinary arena play the objective panel now rotates five concise tips covering
    ghost points, side ambushes, two-tap bombs, jumping and yellow-zone banking.
    Guardian and safe-zone messages retain priority over the rotation.
  status: OPEN
  handoff: >
    Confirm the five-second cadence is readable while driving and that each tip
    fits the objective panel at the target tablet viewport.
- date: '2026-08-28'
  agent: Codex
  action: DELIVERED
  requirements: [CHOMP-SYS-032, CHOMP-SYS-055]
  summary: >
    Release 0.3.9-alpha replaces the large rectangular jump control with a
    compact 128-pixel circular button, clipped rising charge fill and four-pixel
    neon readiness ring. Roblox chat is explicitly enabled and right-aligned,
    with a retried legacy SetCore fallback for Studio clients.
  status: OPEN
  handoff: >
    Verify the button remains a comfortable touch target on iPad and that both
    modern TextChatService and the current Studio chat open at the upper right.
- date: '2026-08-27'
  agent: Claude (local session)
  action: DECIDED
  requirements: [CHOMP-SYS-032, CHOMP-SYS-055]
  summary: >
    Action buttons moved to the bottom-right corner and a cycle button added
    (D-CHOMP-071). Jump 128px in the corner matching Roblox's own touch default,
    cycle 96px beside it, left half left entirely to the floating stick. Keyboard
    X cycles and C or E jumps - the jump had no keyboard binding at all before
    today. Locked to landscape; CHOMP-SYS-032 amended to say so. control_scheme.md
    rewritten from the D-CHOMP-033 grid scheme it had been describing since
    D-CHOMP-042 replaced it.
  status: OPEN
  handoff: >
    The rule that replaced "no action buttons": a button must be a verb the
    player already knows they have. Jump and swap qualify; banking and buying do
    not, because driving somewhere already expresses them. Anything modal or
    context-dependent fails it.
    KNOWN DEFECT against CHOMP-SYS-032, recorded rather than hidden: 24 of the
    HUD's 65 UDim2 calls are pure pixel offsets and there is not one
    UISizeConstraint or aspect check in the file. The layout has therefore never
    been verified at any aspect ratio but the one it was authored on. Landscape
    lock reduces the exposure; it does not remove it. Fix before assuming the
    HUD works on a phone or a different iPad.
    Cycle deliberately does NOT skip to the item with most charges. The belt is
    spent in order (D-CHOMP-062) and predictable beats optimal for a child.
