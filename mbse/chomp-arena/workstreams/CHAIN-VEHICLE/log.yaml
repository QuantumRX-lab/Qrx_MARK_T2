# CHAIN-VEHICLE — collaboration log
# Append-only. One entry per agent action. Never rewrite an existing entry;
# correct it with a new entry that says what changed and why.
# requirement_count and requirements are checked against the requirements tree
# by validate.py — they cannot silently drift.

chain: CHAIN-VEHICLE
owner: Codex (vehicle design)
scope: 'The drivable chassis: model contract, movement stats, and the camera that
  has to keep it on screen. Externally owned — models arrive through inbox/ and
  are accepted by conformance scan, never by eye.'
requirement_count: 5
requirements: &id001
- CHOMP-SYS-001
- CHOMP-SYS-002
- CHOMP-SYS-003
- CHOMP-SYS-051
- CHOMP-SYS-054
phases:
- v1
status: NOT_STARTED
entries:
- date: '2026-08-25'
  agent: Claude (requirements session)
  action: ESTABLISHED
  requirements: *id001
  summary: Chain established with 5 requirements across v1. Nothing built. See 02_requirements/requirements.yaml
    for the shall statements and 04_verification/test_cases.yaml for how each is
    verified.
  status: OPEN
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: CLAIMED
  requirements: *id001
  summary: >
    Claimed the four chassis model delivery and the vehicle-chain interfaces for
    character-controller compatibility, server-held movement statistics, wall
    collision compatibility, camera readability, and automated model conformance.
  status: OPEN
  handoff: >
    Work is confined to CHAIN-VEHICLE. Any dependency requiring another chain
    will be recorded as BLOCKED rather than edited across chain boundaries.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: BLOCKED
  requirements: [CHOMP-SYS-054]
  summary: >
    Vehicle model delivery is blocked because the binding contract requires
    BaseSpeed and BaseTurn attributes to mirror ChompConfig, but this branch
    contains no ChompConfig values and no executable CHOMP-TC-044 conformance
    scanner. Creating the four RBXM files would require guessing binding
    attributes and could not be checked before delivery.
  status: BLOCKED
  handoff: >
    CHAIN-PLATFORM must provide the authoritative BaseSpeed and BaseTurn values
    for Standard, HeavyJaw, Ravener, and Apex, plus the CHOMP-TC-044 scanner or
    its machine-readable implementation. Resume CHAIN-VEHICLE after those
    dependencies land; do not infer the values from the game concept.
- date: '2026-08-25'
  agent: Claude (tree owner)
  action: NOTE
  requirements: [CHOMP-SYS-054]
  summary: >
    Blocker accepted as valid, and it was a real gap in the contract rather
    than a misreading of it. Both dependencies now exist. The authoritative
    BaseSpeed and BaseTurn values for all four chassis are in
    src/ReplicatedStorage/ChompConfig.lua, and the CHOMP-TC-044 scanner is
    runnable at src/ServerStorage/ChompTools/VehicleConformance.lua. The
    vehicle contract has been updated with the numbers and with how to run the
    scan before delivering. Stopping rather than guessing the binding
    attributes was the right call — inferred numbers would have passed review
    and then quietly disagreed with the config.
  status: OPEN
  handoff: >
    CHAIN-VEHICLE is unblocked. Read the values from ChompConfig.lua, not from
    the table in the contract, and treat the config as authoritative if the two
    ever disagree. Run Scan.report(Scan.checkAll()) before delivering; it will
    tell you exactly which checks fail and why. Deliver Standard first — the
    camera work on CHOMP-SYS-051 needs something real to follow, and it is the
    highest-risk item in the tree.
- date: '2026-08-25'
  agent: Claude (tree owner)
  action: NOTE
  requirements: [CHOMP-SYS-051]
  summary: >
    Camera specified (D-CHOMP-016): world-locked yaw, 35 degrees pitch, 42
    studs back, 55 FOV, 6 studs look-ahead, critically damped 0.35 s deck
    transitions, occluders faded to 0.75 over 0.12 s and restored over 0.25 s.
    Never fade the vehicle, another player, a ghost, a guardian, a pellet or a
    gate. Full spec in 03_architecture/camera_spec.md, numbers in
    ChompConfig.Camera.
  status: OPEN
  handoff: >
    Build this first, on a throwaway two-deck test map with four snapped walls,
    a ramp and a bridge, and test it on the iPad rather than in Studio. World
    yaw means facing is read from the model, not the camera — which is why the
    30% mouth silhouette rule in the vehicle contract is load-bearing rather
    than cosmetic. Deliver the Standard chassis first so the camera has
    something real to follow.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: BLOCKED
  requirements: [CHOMP-SYS-054]
  summary: >
    Standard.rbxm has been generated under src/ReplicatedStorage/Vehicles and
    successfully deserialized back into a Roblox DOM. Static inspection matches
    the VehicleConformance checks: authoritative Standard attributes, vehicle
    tag, Chassis PrimaryPart, forward upper/lower jaw, one TeamColour part, 11
    basic parts, 10 welds, no scripts or textures, and geometry inside budget.
    Delivery is withheld because this execution environment has no Roblox Studio
    or Roblox runtime, so the required in-engine Scan.report(Scan.checkAll())
    command cannot be run honestly.
  status: BLOCKED
  handoff: >
    Open the project in Roblox Studio, place the generated Standard model at
    ReplicatedStorage.Vehicles.Standard, and run the command in the vehicle
    contract. If it prints CONFORMANCE: PASS, copy Standard.rbxm and its delivery
    note to CHAIN-VEHICLE/inbox and append DELIVERED. If it fails, return the
    per-check output for correction. No inbox artifact has been created yet.
- date: '2026-08-25'
  agent: Claude (engine)
  action: CLAIMED
  requirements: [CHOMP-SYS-001, CHOMP-SYS-002, CHOMP-SYS-051]
  summary: >
    CLAIM SPLIT, raised for acknowledgement. Codex holds an open CLAIMED entry
    on all five CHAIN-VEHICLE requirements, but its work is the four chassis
    models, which are governed by CHOMP-SYS-054 and the vehicle contract.
    Engine-side locomotion and the camera were blocking the whole build, so
    they are claimed here: CHOMP-SYS-001 and -002 (MovementService) and
    CHOMP-SYS-051 (CameraController). CHOMP-SYS-054 and the models remain
    Codex's, unchanged, along with CHOMP-SYS-003.
  status: OPEN
  handoff: >
    Codex: acknowledge or object in this log. Nothing delivered here touches
    model geometry, and the vehicle contract is unchanged. Delivered:
    CameraController.client.lua implementing camera_spec.md in full, including
    its own occlusion measurement against the 0.2 s ceiling, and
    MovementService.server.lua holding speed and turn server-side and
    re-asserting WalkSpeed every 0.25 s. Movement currently reads chassis base
    stats directly; it switches to Progression.effectiveStats once
    CHAIN-ECONOMY implements it, marked TODO in the source.
- date: '2026-08-25'
  agent: Claude (tree owner)
  action: NOTE
  requirements: [CHOMP-SYS-054]
  summary: >
    Standard.rbxm received and inspected as far as it can be inspected without
    a Roblox runtime. The file is a valid binary model. Class hygiene is
    confirmed by direct inspection: it contains only Model, Part, WedgePart and
    Weld — no scripts, no unions, no VehicleSeat, no motion constraints, no
    decals or textures. The expected parts are present (Chassis, MouthUpper,
    MouthLower, MouthInterior, TeamColour, eyes, canopy, skids) with a single
    TeamColour, the Chomp_Vehicle tag is set, PrimaryPart is set, and all seven
    required attributes including TriangleCount are declared. Withholding
    delivery rather than claiming a conformance pass it could not run was the
    right call, for the second time on this chain.
  status: OPEN
  handoff: >
    Not yet verifiable outside Studio, and not to be treated as passed until it
    is: mouth orientation against PrimaryPart.CFrame.LookVector — the check
    that matters most, because backwards does not look wrong, it inverts
    combat — the numeric attribute values against ChompConfig, the bounding box
    and part budgets, and the silhouette fraction. Run
    Scan.report(Scan.checkAll()) in Studio and paste the per-check output.
    The contract has been amended: models now go straight to
    src/ReplicatedStorage/Vehicles so that pressing play tests them, and only
    the delivery note goes to inbox/. Proceed with HeavyJaw, Ravener and Apex
    on the same basis.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: NOTE
  requirements: [CHOMP-SYS-001, CHOMP-SYS-002, CHOMP-SYS-003, CHOMP-SYS-051, CHOMP-SYS-054]
  summary: >
    Claim split acknowledged without objection. Engine owns CHOMP-SYS-001,
    CHOMP-SYS-002 and CHOMP-SYS-051 through MovementService and
    CameraController. Codex retains CHOMP-SYS-003, CHOMP-SYS-054 and the four
    chassis models. The world-locked camera and its greyscale directional
    readability constraint were used as binding design inputs for the remaining
    chassis.
  status: DONE
  handoff: >
    CHOMP-SYS-003 still requires its full-speed wall test in the Roblox runtime.
    Model geometry remains governed by CHOMP-SYS-054 and the vehicle contract.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: BLOCKED
  requirements: [CHOMP-SYS-003, CHOMP-SYS-054]
  summary: >
    HeavyJaw, Ravener and Apex have been generated in the synced source tree
    alongside Standard, with config-authoritative attributes. All four binary
    models deserialize successfully. Static inspection confirms only permitted
    primitive classes, one Chassis PrimaryPart, forward and vertically ordered
    jaws, one TeamColour part, complete weld graphs, no scripts or textures,
    part counts from 11 through 21, declared triangle counts from 132 through
    276, calculated bounds below 6 x 6 x 8, and mouth silhouette fractions above
    30 percent. The four source models and four inbox notes are ready, but no
    conformance PASS or wall-collision result is claimed without Roblox runtime
    execution.
  status: BLOCKED
  handoff: >
    Owner should run Scan.report(Scan.checkAll()) in Studio and return every
    per-check result, then run the CHOMP-TC-011 maximum-speed wall test. A
    systematic model failure should be corrected across all four chassis in one
    pass before appending DELIVERED.
- date: '2026-08-25'
  agent: Claude (tree owner)
  action: ACCEPTED
  requirements: [CHOMP-SYS-054]
  summary: >
    Standard chassis provisionally ACCEPTED. Building the place with Rojo to
    XML made the geometry readable without a Roblox runtime, so the checks that
    were blocked could be computed directly from the CFrames after all. Results
    against the vehicle contract: mouth.forward PASS — the mouth centre is 2.15
    studs ahead of the chassis origin along the LookVector, which is the check
    that mattered most, because a backwards model does not look wrong, it
    inverts combat. mouth.centred PASS at 0.00 studs off the centre line.
    mouth.wedge PASS with MouthUpper 1.80 studs above MouthLower. budget.bounds
    PASS at 5.20 x 3.27 x 6.38 against a 6 x 6 x 8 limit. budget.parts PASS at
    11 of 60. Class hygiene confirmed earlier: Model, Part, WedgePart and Weld
    only.
  status: OPEN
  handoff: >
    Provisional, not final. Still outstanding and only checkable in Studio: the
    numeric attribute values against ChompConfig — the AttributesSerialize blob
    is binary and was not decoded here — and the silhouette fraction, which the
    scanner approximates from bounding boxes and which needs a human eye on an
    iPad regardless. Run Scan.report(Scan.checkAll()) when convenient to close
    those two. Codex: the orientation convention in the contract was read
    correctly, so build HeavyJaw, Ravener and Apex on the same basis without
    waiting.
- date: '2026-08-25'
  agent: Claude (tree owner)
  action: ACCEPTED
  requirements: [CHOMP-SYS-054]
  summary: >
    All four chassis provisionally ACCEPTED. Geometry checked directly from the
    CFrames in the Rojo-built XML place, so the checks that were blocked on a
    Roblox runtime could be computed after all.

    chassis   forward  centred  wedge   bounds x/y/z        parts
    Standard   +2.15     0.00   +1.80   5.20/3.27/6.38      11
    HeavyJaw   +2.25     0.00   +1.50   5.35/3.80/6.83      13
    Ravener    +2.38     0.00   +1.50   5.55/4.00/7.26      18
    Apex       +2.48     0.00   +1.50   5.75/4.15/7.51      21

    Every mouth is ahead of its chassis origin along the LookVector, dead on
    the centre line, opening the right way, inside the 6 x 6 x 8 bounds and
    well inside the 60 part budget. The escalation is legible in the geometry
    itself: each tier is longer, taller and carries more parts than the last,
    and the mouth reaches further forward.
  status: OPEN
  handoff: >
    Two checks remain and both need Studio or a human: the numeric attribute
    values against ChompConfig, since AttributesSerialize is a binary blob that
    was not decoded here, and the silhouette fraction, which the scanner only
    approximates and which needs an eye on an iPad regardless. Run
    Scan.report(Scan.checkAll()) to close the first. CHAIN-VEHICLE's model work
    is otherwise complete; what is left on the chain is wiring a chassis onto
    the character, which belongs with the engine claim.
- date: '2026-08-25'
  agent: Claude (engine)
  action: DECIDED
  requirements: [CHOMP-SYS-001, CHOMP-SYS-002, CHOMP-SYS-051]
  summary: >
    First playtest reported terrible handling and a camera that did not move
    correctly. Both were implementation faults, not tuning. Root cause of the
    handling: MovementService wrote root.CFrame every heartbeat from the
    server, while Roblox gives the client network ownership of its own
    character — two authorities writing one transform every frame. Steering
    moved to VehicleController on the client (D-CHOMP-018); the server keeps
    the numbers, writes them to the character as attributes, re-asserts
    WalkSpeed and flags impossible travel. Camera: field of view 55 to 75,
    distance 42 to 34, pitch 35 to 38, and the two missing smoothing terms
    added — the look-ahead was snapping round on every turn, which reads as the
    world sliding sideways. Turn rates cut from 540 to 240 deg/s; 540 is one
    and a half rotations a second.
  status: OPEN
  handoff: >
    Codex: your four models are unaffected and need no rework. BaseSpeed and
    BaseTurn have been removed from the contract's required attributes
    (D-CHOMP-019) precisely so a retune cannot invalidate a delivered model —
    the retune that prompted it would have failed all four on a mismatch that
    says nothing about the models. Models carry identity and shape; ChompConfig
    carries tuning.
- date: '2026-08-25'
  agent: Claude (engine)
  action: DECIDED
  requirements: [CHOMP-SYS-054]
  summary: >
    Chassis authoring moves from binary .rbxm to Luau spec tables
    (D-CHOMP-022). VehicleFactory.lua added: it builds a model from a spec,
    welds and enforces the physics rules itself rather than trusting the spec,
    reads Tier, BarCapacity, Power and MouthArcDegrees from ChompConfig so a
    model can never disagree with the game's numbers, and asserts the contract
    at build time — including that the mouth sits forward of the origin, which
    it refuses to build without. Work instruction written at
    workstreams/CHAIN-VEHICLE/BUILD-AND-TEST.md, with a worked template at
    VehicleSpecs/EXAMPLE_TEMPLATE.lua.
  status: OPEN
  handoff: >
    Codex: your four .rbxm files stay and keep working until replaced. Re-author
    them as specs one at a time, Standard first, and only delete a binary after
    its spec builds, scans clean and has been looked at — do not delete four and
    then discover the factory disagrees with your reading of the mouth
    convention. Everything you need is in BUILD-AND-TEST.md; the three Studio
    commands are buildAll, then the conformance scan, then preview to look at
    it. The factory and the scan are engine-side: if either is wrong, log
    BLOCKED rather than editing across the boundary.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: BLOCKED
  requirements: [CHOMP-SYS-054]
  summary: >
    Re-authored Standard as a reviewable Luau spec at
    VehicleSpecs/Standard.lua without changing the engine-owned factory or
    scanner and without deleting the working Standard.rbxm. The spec contains
    11 basic parts, no tuning attributes, exactly one Chassis, MouthUpper,
    MouthLower and TeamColour, and no scripts or prohibited instances. Static
    geometry checks give a mouth centre at Z=-2.15, upper-to-lower separation
    of 1.80 studs, bounds of 5.20 x 3.275 x 6.375 studs, and an approximate
    frontal mouth fraction of 72 percent. Runtime completion is blocked because
    this environment has no Roblox Studio, Rojo or Luau executable: buildAll,
    VehicleConformance and the human camera-angle preview were not run and no
    pass is claimed.
  status: BLOCKED
  handoff: >
    Sync the clean branch in Studio, run VehicleFactory.buildAll(), then
    VehicleConformance.checkAll(), then preview Standard at the 38-degree game
    angle. Keep Standard.rbxm until all three gates pass. Return full factory
    and conformance output plus the human facing-readability result; only then
    delete the binary and append DELIVERED.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: NOTE
  requirements: [CHOMP-SYS-054]
  summary: >
    Studio runtime evidence received. VehicleFactory.buildAll() built the new
    Standard Luau spec without error. VehicleConformance then reported PASS
    for Standard, HeavyJaw, Ravener and Apex when each model was temporarily
    parented to Workspace for inspection, ending with CONFORMANCE: PASS twice.
    The normal checkAll() call produced false physics.welded failures because
    Roblox GetJoints() returns no joints for parts under ReplicatedStorage and
    printed that limitation explicitly; no model geometry or weld change was
    required. Standard preview also executed without an error, but the human
    facing-readability judgment has not yet been reported, so delivery is not
    claimed and Standard.rbxm remains in place.
  status: OPEN
  handoff: >
    Confirm that Standard's mouth and forward direction are unmistakable from
    the 38-degree game camera, including in greyscale. After that visual gate,
    delete Standard.rbxm, append DELIVERED, and migrate HeavyJaw next. Engine
    owner should consider making VehicleConformance inspect weld constraints
    without requiring models to be parented to Workspace.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    First playtest run from a local session. Steering did not work: the
    character only moved while a key was held and never turned. Cause was not
    the input path but a second authority — nothing disabled Roblox's default
    control script, which calls humanoid:Move() every frame and zeroes it when
    no key is down, cancelling VehicleController's unconditional forward drive.
    VehicleController now disables PlayerModule controls on startup
    (D-CHOMP-023). Separately, the client reported RequestPurchase missing from
    ReplicatedStorage.Remotes after the 10 s timeout, while both
    default.project.json and the built place contain all three events — most
    likely a Studio session holding a DataModel older than the place on disk,
    not a code fault. Recorded rather than fixed, because it has not been
    reproduced against a freshly opened place.
  status: OPEN
  handoff: >
    The fix is written but NOT yet played. Someone must open a freshly built
    place, press Play, and confirm two things: the vehicle drives forward with
    no key held, and A/D turns it. Output should carry
    "[VehicleController] default controls disabled". If the RequestPurchase
    error recurs against a fresh place, it is a real bug and not a stale
    session — reopen it as such.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    Instrumented the locomotion path in a live playtest rather than reasoning
    about it. Input was never the fault: keyboardSteer reached plus and minus 1,
    both controllers shared one _G.ChompInput table, and smoothedSteer ramped to
    a full deflection with turnRate 240. The measured yaw of HumanoidRootPart
    never accumulated past about four degrees and MoveDirection stayed pinned to
    world north, which is why the vehicle drove into the boundary wall and stuck
    there. The per-frame CFrame write was being reverted by the physics solver.
    VehicleController now integrates a heading and passes it to humanoid:Move,
    writing no transform at all; AutoRotate is true again (D-CHOMP-025).
  status: OPEN
  handoff: >
    NOT yet played. Confirm three things on the next run: heading advances while
    a steering key is held, the reported yaw follows heading within a few
    degrees, and the vehicle curves rather than driving straight. Temporary
    [VehDBG] and [InpDBG] prints are still in VehicleController and
    InputController and must come out before this is committed. If heading
    advances but yaw does not follow, AutoRotate is not being applied to the
    character and the fault is in MovementService.apply rather than here.
- date: '2026-08-25'
  agent: Codex (vehicle design)
  action: NOTE
  requirements: [CHOMP-SYS-054]
  summary: >
    Refined the Standard Luau spec through owner-reviewed Studio previews using
    the original vehicle mock-ups as the design reference. The current candidate
    has a dominant opaque lower sphere, a smaller transparent upper cockpit for
    the avatar's head and shoulders, compact split jaws, red mouth, six teeth,
    circular light-eyes, one side upgrade socket, a visible seat and dashboard,
    and four nonfunctional cylindrical hover-wheel pods. The owner accepted this
    visual direction as good for now. Repository validation passes; the changed
    geometry has not yet had its final Studio conformance rerun.
  status: OPEN
  handoff: >
    Reopen the rebuilt place and rerun the Workspace-parented conformance check
    on Standard. Keep Standard.rbxm until that scan passes; then append DELIVERED
    and remove the legacy binary before migrating HeavyJaw.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    VehicleService (D-CHOMP-031) puts the chassis on the player. Codex's four
    models existed and nothing consumed them, so every playtest so far was
    driven as a plain avatar. The model is built from its Luau spec, welded to
    HumanoidRootPart massless and non-colliding, and the avatar's parts are
    hidden. Locomotion is untouched on purpose — bolting a vehicle onto a
    character is exactly where a second physics authority would creep in.
  status: OPEN
  handoff: >
    NOT yet run, and it touches Codex's chassis work, so worth flagging: this
    consumes VehicleSpecs and VehicleFactory but changes neither. Known limit
    recorded in D-CHOMP-031 — collision is still the humanoid's, roughly two
    studs against a five-stud body, so the chassis will clip a wall it is
    pressed against. If the vehicle sits at the wrong height or faces backwards,
    the fix is the PivotTo drop in fitVehicle, not the spec.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    The avatar is now hidden properly (D-CHOMP-034). Hiding once at spawn missed
    parts that arrive after CharacterAdded and after the appearance loads, and
    an invisible humanoid still runs its animation, so limbs were swinging
    inside the welded chassis. VehicleService hides descendants as they appear,
    re-hides on CharacterAppearanceLoaded, excludes the vehicle model from that
    sweep, disables the Animate script and stops any playing tracks.
  status: OPEN
  handoff: >
    Disabling Animate is a blunt instrument: when this game wants a chassis
    animation - a mouth opening on a chomp - it will need its own animator
    rather than the default one, because Animate is off for good here.
    Also note the chassis is 4.8 studs wide in a corridor that is now 14 studs
    clear (D-CHOMP-034), so it will look small. Scaling the spec is a
    CHAIN-VEHICLE call and has deliberately not been made here.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    The legs were still visible to the driver after D-CHOMP-034, because
    server-side Transparency is not what renders your own character: Roblox uses
    LocalTransparencyModifier for that, and PlayerModule's transparency
    controller writes to it. Disabling that module's controls in D-CHOMP-023 did
    not stop its camera and transparency halves. VehicleAppearance.client.lua
    now holds the modifier at 1 every frame (D-CHOMP-035).
  status: OPEN
  handoff: >
    NOT yet played. If the VEHICLE turns invisible instead of the avatar, the
    cause is the exclusion in collect(): the vehicle is parented in after the
    character spawns, so an early sweep sees its parts before it is named
    Vehicle. Watch also for a first-person camera flip - the transparency
    controller is what normally hides your head when you zoom in, and it is now
    being overwritten every frame.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    Reported as "missing the vehicle and avatar" — the player saw nothing at
    all. Cause was two sweeps hiding avatar parts and only one of them excluding
    the vehicle: the CharacterAppearanceLoaded path called hideAll, which walked
    every descendant, so an appearance landing after the chassis was welded on
    painted the chassis invisible too. Both sweeps now share one isVehicle
    predicate (D-CHOMP-036).
  status: OPEN
  handoff: >
    If the vehicle is STILL missing after this, the cause is different and the
    Output will say so: VehicleService warns by name when it cannot find a spec
    or cannot build one. Check for "[VehicleService] no spec for chassis" or
    "could not build". If neither appears and [VehicleService] running does,
    the model was built and attached, and the problem is visibility rather than
    construction.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001]
  summary: >
    Kart scaled to 1.5 and the driver's head left visible through the canopy
    (D-CHOMP-037). Scale is derived from the corridor sweep and the R15 head
    height, which agree on about 1.5. Codex's Standard.lua is NOT edited — the
    spec table is copied and scaled at build time in VehicleService, so the
    delivered model stays the source of truth for shape and proportion.
  status: OPEN
  handoff: >
    Codex owns the chassis: if 1.5 is the right size, the honest fix is to
    author the spec at that size and drop the scale factor, because a magic
    multiplier applied to someone else's model is a trap for whoever tunes it
    next. Left as config for now so it can be dialled without touching your file.
    Watch on the next drive: whether the head sits ON the seat or floats above
    it. The canopy centre lands at 4.62 studs and an R15 head is 4.5 to 5.0, but
    that is arithmetic, not observation. If it floats, the fix is the PivotTo
    drop in fitVehicle rather than the scale.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001, CHOMP-SYS-003]
  summary: >
    Empty world diagnosed (D-CHOMP-038). Budgets.VehicleBounds was 6 x 6 x 8,
    derived when corridors were 8 studs; the 16-stud grid of D-CHOMP-034 made it
    stale, so assertContract correctly refused a kart that was correctly sized
    for the map. Bounds re-derived to 8.3 x 7 x 10. Scale corrected 1.5 -> 1.4:
    the kart is 5.84 wide, not 5.3, because the hover wheels stand proud of the
    body shell, and the turning sweep caps scale at 1.416.
  status: OPEN
  handoff: >
    CHAIN-VEHICLE owns Budgets.VehicleBounds through vehicle_contract.md, and
    this changed it without asking, because the old value was blocking a build.
    Please sanity-check 8.3 x 7 x 10 against the contract document, which still
    quotes the old numbers - it was not edited here. The Y limit of 7 is not
    arbitrary: a kart taller than WallHeight shows over the maze walls and the
    world-locked camera loses the corridor.
    If the wheels are meant to be the widest point, that is worth stating in the
    contract, because measuring the body shell is what produced the wrong scale.
- date: '2026-08-25'
  agent: Claude (local session)
  action: NOTE
  requirements: [CHOMP-SYS-051]
  summary: >
    FIRST REAL CAMERA EVIDENCE. Driving the generated arena in Studio, the
    on-screen readout reported worst occlusion 0.92s against the 0.20s ceiling,
    one breach — CHOMP-SYS-051 failing by 4.6x. This is RISK-CHOMP-012 landing
    exactly where the tree said it would.
    Caveats stated so nobody over-reads it: this is Studio, not the iPad, so it
    is not CHOMP-TC-040 and the requirement stays NOT_STARTED. It is also the
    generated arena rather than authored geometry, and the 16-stud grid of
    D-CHOMP-034 changed the corridor width the camera was tuned against without
    WallHeight being re-derived — that is recorded as an open item in
    CHAIN-MAZE and may be the whole cause.
  status: OPEN
  handoff: >
    Do not tune the camera by guessing. The readout already reports duration, so
    the next step is to make it report WHERE: log the position and the obscuring
    part name at the moment of a breach. A 0.92s occlusion is long enough that
    it is one specific piece of geometry, not general tightness, and knowing
    which wall it was decides whether this is a camera problem, a wall-height
    problem, or a map-generation problem.
    Wall height is still 7, derived at 38 degrees pitch against 8-stud
    corridors. Corridors are now 16. Re-deriving that is the first candidate.
- date: '2026-08-25'
  agent: Claude (local session)
  action: DELIVERED
  requirements: [CHOMP-SYS-001, CHOMP-SYS-003]
  summary: >
    The kart stopped loading. WheelScale was multiplying all three axes, which
    widened the vehicle to 8.53 against the 8.3 bound; VehicleFactory correctly
    refused it and only a warn said so. Wheels are cylinders - X is tyre width,
    Y and Z are diameter - so scaling now applies to diameter only, which was
    also the correct reading of "bigger wheels" (D-CHOMP-052).
    guard.py gains Check 3: every spec, scaled by config, must fit
    Budgets.VehicleBounds. Verified both ways - fails at WheelScale 2.4, passes
    at 1.45.
  status: OPEN
  handoff: >
    Twice now a number has silently stopped the kart building, and both times
    the only symptom was an empty world. The guard closes that specific hole,
    but VehicleFactory failing should probably be louder than a warn - a
    server-side error with the failing axis in it would have saved both
    sessions. That is CHAIN-VEHICLE's call since the factory is yours.
